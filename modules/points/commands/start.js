module.exports = {
    name: 'point_start',
    description: 'Grants access to the point system. User ID is collected',
    syntax: 'point_start',
    num_args: 0,
    args_to_lower: false,
    needs_api: true,
    has_state: false,
    async execute(message, args, extra) {
    }
}